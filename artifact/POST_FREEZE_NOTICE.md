# Post-freeze notice

`README.md`, `RIGHTS.md`, and the other files listed in `FROZEN_MANIFEST.json` are preserved byte-for-byte as the inputs frozen before the original run. Their historical wording includes an intention to publish the original observations.

That intention was superseded after the run when the researchers recognized that the invitation had not explicitly requested participation and publication authorization from each invocation. The frozen files were not rewritten. Instead:

- the exact original `observations.jsonl` was excluded from this repository;
- `runs/20260816T110618Z-sonnet/` retains aggregates and cryptographic commitments;
- `informed_consent/` records a prospectively frozen, invocation-scoped authorization replication; and
- the repository-root `README.md` and `RIGHTS.md` state the final public boundary.

This notice is post-freeze metadata. It is not part of the original preregistration and does not alter any frozen hash.
